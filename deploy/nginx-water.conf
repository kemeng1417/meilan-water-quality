# 水质管理系统 Nginx 配置
# 将此文件放到 /etc/nginx/sites-available/ 并创建软链接到 sites-enabled/

# === 方案A：独立子域名（推荐，取消注释使用）===
# server {
#     listen 80;
#     server_name water.your-domain.com;
#     return 301 https://$host$request_uri;
# }
#
# server {
#     listen 443 ssl;
#     server_name water.your-domain.com;
#
#     ssl_certificate     /etc/ssl/certs/water.pem;
#     ssl_certificate_key /etc/ssl/private/water.key;
#
#     location / {
#         proxy_pass http://127.0.0.1:8000;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#     }
#
#     client_max_body_size 20m;
# }

# === 方案B：URL 路径前缀 /water/（与现有站点共存）===
# 注意：使用此方案需要同时修改前端 baseURL
#
# location /water/ {
#     rewrite ^/water/(.*) /$1 break;
#     proxy_pass http://127.0.0.1:8000;
#     proxy_set_header Host $host;
#     proxy_set_header X-Real-IP $remote_addr;
#     proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#     proxy_set_header X-Forwarded-Proto $scheme;
# }

# === 方案C：独立端口（最简单，直接访问 47.120.66.1:8000）===
# 无需额外 Nginx 配置，但需要开放防火墙端口
